# Vision

Goal:
- Build a SaaS similar to Shopify while using WooCommerce as the commerce engine.
- Treat WordPress as a runtime, not the business platform.

Architecture:
Control Plane (NestJS)
Management Plane (Go Agent)
Runtime Plane (WordPress Multisite + WooCommerce + HyperDB)
