Read `AGENTS.md`, then run the AI-Kit workspace bootstrap from the project root.

```bash
ai-kit setup
ai-kit validate
ai-kit status
```

Do not use `--force` or delete `.ai-work` unless the user explicitly requests
that operation. Report the created or refreshed bridge files and validation
result.
