Read and follow `AGENTS.md`. For an implementation request, run
`ai-kit route <task-id>` and `ai-kit context <task-id>`, claim the assigned task
through the control plane, implement only its scope, run focused verification,
and record evidence. Do not hand-edit lifecycle JSON.
